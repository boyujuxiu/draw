# AGENTS.md

## 项目结构

- `Untitled Diagram.drawio`：draw.io 图表源文件。
- `hello world`：空白文本文件。

## 项目目标

- 先理解现有代码和项目结构，再开始修改。
- 优先进行小范围、可验证的修改。
- 不做与当前任务无关的重构、格式化或清理。

## 技术栈与约束

- 使用项目现有的技术栈和依赖版本。
- 不要主动升级依赖，除非任务明确要求。
- 遵循项目现有的代码风格、目录结构和命名规范。
- 不引入新的框架、库或抽象，除非现有方案无法满足需求。
- 不修改公开 API、配置格式或数据库结构，除非任务明确要求。

## 计划模式

- 对复杂任务先制定计划，明确目标、范围、验收标准、风险和验证方式。
- 计划模式用于帮助任务负责人自检，不构成团队审批流程。
- 每个任务默认只创建一个计划文件。
- 临时计划统一放在 `.agent/plans/`，不要在项目根目录散落 Markdown 文件。
- 任务负责人可以在完成自检后直接开始实施，不需要等待其他团队成员批准。
- 如果当前用户明确要求“先给方案，等我确认”，则暂停实施并等待确认。
- 只有出现以下情况时才必须暂停并询问：
  - 产品目标不明确；
  - 存在相互冲突的要求；
  - 需要用户提供无法推断的信息；
  - 操作可能造成不可逆的数据损失；
  - 需要修改公共接口、数据库结构或其他团队正在依赖的契约。
- 实施完成后，更新计划中的验证结果和遗留问题。
- 只有长期有效的架构决策，才整理到 `docs/decisions/`。

## 开发流程

1. 先检查相关代码、配置和测试。
2. 对复杂任务先给出简短实施计划。
3. 先实现最小可行修改。
4. 修改后运行相关测试、构建和检查命令。
5. 最后总结修改内容、验证结果和仍存在的风险。

## 编码原则

- 保持实现简单、清晰、可维护。
- 优先解决根本原因，不使用临时补丁。
- 复用现有函数、组件和工具。
- 避免过度设计和不必要的抽象。
- 错误处理应保留足够的上下文。
- 不泄露密码、密钥、Token 或其他敏感信息。

## 测试与验证

- 新增或修改功能时，补充相关测试。
- 测试应覆盖正常流程、异常流程和重要边界条件。
- 修复 Bug 时，先添加能够复现问题的测试。
- 修改后至少运行与改动相关的测试。
- 如果无法运行测试，必须说明原因。

## 文档

- 修改配置、接口、命令或使用方式时，同步更新相关文档。
- 不为了形式更新 README，只在内容确实发生变化时更新。
- 新增环境变量时，更新示例配置或环境变量说明。
- 每次功能或代码变更后，检查 `README.md` 和 `AGENTS.md` 是否仍然准确；只有事实发生变化时才同步更新。

## Git 与变更范围

- 每次修改应保持单一目的。
- 不修改与当前任务无关的文件。
- 不删除已有代码，除非能够证明它已经无用或任务明确要求。
- 提交前检查变更内容，确认没有调试代码、临时文件或敏感信息。
- GitHub 远端发布前使用明确文件清单审计暂存对象和待推送历史；禁止使用 `git add .`、`git add -A`、强制推送或未授权的历史重写。

## 完成标准

任务只有在以下条件满足后才能视为完成：

- 代码已实现需求。
- 相关测试已通过。
- 构建或静态检查已通过。
- 文档和配置已同步更新。
- 没有引入明显的无关改动。

## 权限边界

- 不要因为模型建议执行某项操作，就自动扩大权限。
- 未经我在本次请求中明确授权，不得提交Git、推送远程、创建PR、部署、迁移数据库、修改线上配置、启用生产功能或操作真实用户数据。

## 最终报告

- 实际修改内容。
- 独立测试结果。
- 仍未验证的风险。
- 当前代码究竟只是本地修改，还是已经提交、推送或部署。
